<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark Mode (IG Style) -->
    <style name="Base.Theme.AppSplitBill" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue_primary</item>
        <item name="android:windowBackground">@color/background_default</item>
        <item name="android:statusBarColor">@color/background_default</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Surface colors for cards/dialogs -->
        <item name="colorSurface">@color/surface_default</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>
    </style>
</resources>